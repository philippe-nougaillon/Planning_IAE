<div class="pb-2 mt-6 mb-2 border-t">
  <% if current_user.admin? %>
    <div class="btn-group float-right">
      <%= link_to fermetures_path, class: 'btn btn-sm btn-primary btn-outline' do %>
        <%= fa_icon 'list' %> Retour
      <% end %>
    </div>
  <% end %>
</div>

<h2>Modification d'un jour de fermeture</h2>

<%= render 'form' %>
