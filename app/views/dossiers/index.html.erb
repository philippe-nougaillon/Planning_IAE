<p id="notice"><%= notice %></p>

<h1>Dossiers</h1>

<table class="table">
  <thead>
    <tr>
      <th>Intervenant</th>
      <th>Période</th>
      <th>Workflow state</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dossiers.each do |dossier| %>
      <tr>
        <td><%= dossier.intervenant_id %></td>
        <td><%= dossier.période %></td>
        <td><%= dossier.workflow_state %></td>
        <td><%= link_to 'Voir', dossier %></td>
        <td><%= link_to 'Modifier', edit_dossier_path(dossier) %></td>
        <td><%= link_to '[X]', dossier, method: :delete, data: { confirm: 'Etes-vous bien certain.e ?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dossier', new_dossier_path %>
