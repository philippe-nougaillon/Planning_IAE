<%= bootstrap_form_with(model: document, method: :post, local: true) do |form| %>
    <%= form.hidden_field :dossier_id %>
    <%= form.select :nom, options_for_select(Document.types, params[:nom]), label: 'Type du document' %>
    <%= form.file_field :fichier, required: true, label: 'Fichier PDF', accept: 'application/pdf'  %>
    <%= form.submit "Ajouter", class: "btn btn-success" %>
<% end %>

<script type="application/javascript">
    $('input[type="file"]').change(function(e){
        var fileName = e.target.files[0].name;
        $('.custom-file-label').html(fileName);
    });
</script> 
