<h1 class="text-3xl text-base-content mt-6 border-b pb-4"><%= fa_icon 'calendar-day' %> <%= l(Date.today, format: :long).humanize %></h4>

<% if @cours.any?%>
  <% @cours.each do |cour| %>
    <%= render partial: 'ma_session_intervenant', locals: {cour: cour} %>
  <% end %>
<% else %>
  <div class="chat chat-start mt-12 mb-4">
    <div class="chat-bubble chat-bubble-primary text-white">
      <p class="text-xl font-bold">Aucun cours aujourd'hui !</b>
    </div>
  </div>
  <span>Vous n'avez pas de cours planifiés pour aujourd'hui.</span>
<% end %>

<% if user_signed_in? %>
  <%= link_to 'Voir mes prochains cours...', cours_path(start_date: Date.tomorrow, intervenant_id: @intervenant.id, intervenant: @intervenant.nom + " " + @intervenant.prenom), class: 'text-primary hover:underline' %>
<% end %>