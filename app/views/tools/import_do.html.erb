<div class="page-header">
  <h1 class="text-4xl">Résultat de l'import</h1>
  <% if params[:formation_id].present? %>
    <h3 class="text-2xl">
  	  Formation: <%= Formation.find(params[:formation_id]).nom %>
    </h3>
  <% end %>
</div>

<%= simple_format(@stream) %>