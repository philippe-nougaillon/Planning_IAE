<% require 'devise/version' %>
<p class="mb-4"><%= t('.greeting', recipient: @resource.email) %></p>

<p class="mb-4"><%= t('.message') %></p>

<p class="mb-4"><%= t('.instruction') %></p>

<p class="mb-4"><%= link_to t('.action'), unlock_url(@resource, unlock_token: (Devise::VERSION.start_with?('3.') ? @token :@resource.unlock_token)) %></p>
