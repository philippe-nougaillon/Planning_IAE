<% require 'devise/version' %>
<p class="mb-4"><%= t('.greeting', recipient: @resource.email) %></p>

<p class="mb-4"><%= t('.instruction') %></p>
<p class="mb-4"><%= link_to t('.action'), confirmation_url(@resource, confirmation_token: (Devise::VERSION.start_with?('3.') ? @token : @resource.confirmation_token)) %></p>